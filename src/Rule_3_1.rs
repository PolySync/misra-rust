fn main() {
    let x = 0;

    /* End comment marker omitted.
        Perform_Critical_Safety_Function(x)
    /* Non-compliant comment. */

    println!("This program contains comments in a comment.");
}
